<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground">

    <!-- App Bar -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            app:layout_scrollFlags="scroll|enterAlways" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Main Content -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Header: Photo, Meal Type & Date/Time -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="16dp"
                android:gravity="center_vertical">

                <!-- Photo: always visible, two states.
                     EMPTY: tinted background + centered large camera icon,
                       whole container clickable -> showImageSourceDialog().
                     FILLED: the photo (tap -> full-screen viewer, via
                       bindFullScreenTap) + a small camera badge overlaid
                       bottom-right (tap -> showImageSourceDialog() directly,
                       without detouring through the viewer). Avatar-with-
                       edit-badge pattern, same idea as a profile photo. -->
                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/mealImageContainer"
                    android:layout_width="72dp"
                    android:layout_height="72dp"
                    android:layout_marginEnd="12dp"
                    app:cardCornerRadius="12dp"
                    app:cardElevation="0dp"
                    app:strokeWidth="1dp"
                    app:strokeColor="?attr/colorOutlineVariant"
                    app:cardBackgroundColor="?attr/colorSurfaceVariant">

                    <ImageView
                        android:id="@+id/mealImagePlaceholder"
                        android:layout_width="28dp"
                        android:layout_height="28dp"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_camera_add"
                        android:contentDescription="@string/menu_replace_image"
                        android:importantForAccessibility="no" />

                    <ImageView
                        android:id="@+id/mealImage"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:scaleType="centerCrop"
                        android:contentDescription="@string/product_image_description"
                        android:visibility="gone"
                        tools:visibility="visible"
                        tools:src="@drawable/ic_food_placeholder" />

                    <ImageView
                        android:id="@+id/mealImageCameraBadge2"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_gravity="bottom|end"
                        android:layout_margin="3dp"
                        android:background="@drawable/bg_expand_icon_scrim_sm"
                        android:contentDescription="@string/menu_replace_image"
                        android:padding="3dp"
                        android:scaleType="fitCenter"
                        android:visibility="gone"
                        app:srcCompat="@drawable/ic_camera_add"
                        tools:visibility="visible" />

                    <ImageView
                        android:id="@+id/mealImageCameraBadge3"
                        android:layout_width="20dp"
                        android:layout_height="20dp"
                        android:layout_gravity="bottom|end"
                        android:layout_margin="3dp"
                        android:background="@drawable/bg_expand_icon_scrim_sm"
                        android:contentDescription="@string/menu_replace_image"
                        android:padding="3dp"
                        android:scaleType="fitCenter"
                        android:visibility="gone"
                        app:srcCompat="@drawable/ic_camera_add"
                        tools:visibility="visible" />

                    <!-- Standard expand-icon hint, same as every other thumbnail
                         in the app (search cards, hero images) - shown and
                         driven entirely by ImageDisplayUtils.bindFullScreenTap().
                         Replace/remove live in the toolbar menu only, matching
                         ProductDetailsActivity/RecipeDetailsActivity exactly. -->
                    <ImageView
                        android:id="@+id/mealImageExpandIcon"
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:layout_gravity="top|end"
                        android:layout_margin="3dp"
                        android:background="@drawable/bg_expand_icon_scrim_sm"
                        android:padding="3dp"
                        android:src="@drawable/ic_fullscreen"
                        android:scaleType="fitCenter"
                        android:visibility="gone"
                        android:importantForAccessibility="no" />

                </com.google.android.material.card.MaterialCardView>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <!-- Emoji + meal type in one string, one TextView - no
                         second view to align against, so no alignment bug
                         is possible. -->
                    <TextView
                        android:id="@+id/mealTypeText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="?attr/textAppearanceHeadlineMedium"
                        tools:text="🍿 Snack" />

                    <TextView
                        android:id="@+id/mealDateTimeText"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="?attr/textAppearanceBodyMedium"
                        android:layout_marginTop="4dp"
                        tools:text="Friday, January 9, 2026 ⏰ 12:28" />
                </LinearLayout>

            </LinearLayout>

            <!-- Summary Card -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/summaryCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="2dp"
                app:contentPadding="16dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/summary"
                        android:textAppearance="?attr/textAppearanceTitleMedium"
                        android:layout_marginBottom="12dp" />

                    <!-- Nutrient Banners Container -->
                    <LinearLayout
                        android:id="@+id/nutrientBannersContainer"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:weightSum="5" />

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Components Section -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="2dp"
                app:contentPadding="16dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/components"
                        android:textAppearance="?attr/textAppearanceTitleMedium"
                        android:layout_marginBottom="12dp" />

                    <!-- Portions RecyclerView -->
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/portionsRecyclerView"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:nestedScrollingEnabled="false"
                        tools:listitem="@layout/item_meal_portion"
                        tools:itemCount="3" />

                    <!-- Empty State -->
                    <LinearLayout
                        android:id="@+id/emptyPortionsView"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:gravity="center"
                        android:padding="32dp"
                        android:visibility="gone"
                        tools:visibility="visible">

                        <ImageView
                            android:layout_width="64dp"
                            android:layout_height="64dp"
                            android:src="@drawable/ic_food_placeholder"
                            android:alpha="0.3"
                            android:contentDescription="@string/no_items" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/no_items_in_meal"
                            android:textAppearance="?attr/textAppearanceBodyLarge"
                            android:layout_marginTop="8dp" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/tap_add_item_to_start"
                            android:textAppearance="?attr/textAppearanceBodyMedium"
                            android:layout_marginTop="4dp" />

                    </LinearLayout>

                    <!-- Add Item Button -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/addItemButton"
                        style="@style/Widget.Material3.Button.TextButton.Icon"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="12dp"
                        android:text="@string/add_item_to_meal"
                        android:textColor="?attr/colorPrimary"
                        android:paddingTop="14dp"
                        android:paddingBottom="14dp"
                        app:icon="@drawable/ic_add"
                        app:iconTint="?attr/colorPrimary"
                        app:iconGravity="start"
                        app:iconSize="20dp"
                        app:cornerRadius="8dp"
                        app:strokeColor="?attr/colorPrimary"
                        app:strokeWidth="1dp"
                        app:backgroundTint="@android:color/transparent"
                        app:rippleColor="?attr/colorPrimary" />

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Nutrition Information Section -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="2dp"
                app:contentPadding="16dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/nutritional_information"
                        android:textAppearance="?attr/textAppearanceTitleMedium"
                        android:layout_marginBottom="12dp" />

                    <!-- Nutrition Label Container -->
                    <LinearLayout
                        android:id="@+id/nutritionLabelContainer"
                        android:background="?attr/colorSurface"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical" />

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Bottom spacing to account for removed edit button -->
            <View
                android:layout_width="match_parent"
                android:layout_height="16dp" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!-- ========================================================= -->
    <!-- REFRESH FAB - aggregated across every portion's freshness  -->
    <!-- check (see MealRepository.getMealWithFreshnessCheck()).    -->
    <!-- Mirrors activity_product_details.xml/activity_recipe_-     -->
    <!-- details.xml's identical block for a single item.           -->
    <!-- ========================================================= -->
    <LinearLayout
        android:id="@+id/refreshFabContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:visibility="gone">

        <!-- Label on the LEFT -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            app:cardBackgroundColor="@android:color/white"
            app:cardCornerRadius="8dp"
            app:cardElevation="3dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="12dp"
                android:paddingVertical="8dp"
                android:text="@string/refresh_available"
                android:textColor="@android:color/black"
                android:textAppearance="?attr/textAppearanceLabelLarge" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Circular icon FAB on the RIGHT -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/refreshFab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/refresh_available"
            app:srcCompat="@drawable/ic_refresh" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>