<?xml version="1.0" encoding="utf-8"?>
<!-- nutrition_label_row_summary_indented.xml - SUMMARY mode indented row (level 1) -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@android:color/white">

    <!-- Row Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:gravity="center_vertical"
        android:minHeight="32dp">

        <!-- Column 1: Nutrient Name (indented via paddingStart on TextView) -->
        <TextView
            android:id="@+id/nutrientName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:textSize="13sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:paddingStart="16dp"
            android:paddingEnd="8dp"
            android:text="  of which saturated"/>

        <!-- Column 2: Total Value (aligned with standard row) -->
        <TextView
            android:id="@+id/nutrientTotal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="13sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:gravity="end"
            android:text="0 g"/>

    </LinearLayout>

    <!-- Divider under row -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="8dp"
        android:background="?attr/colorOnSurface"
        android:alpha="0.1"/>

</LinearLayout>